import React from 'react';

it('renders without errors/warnings', async () => {

    expect(true).toBeTruthy();//

    // const result = render(<App/>);
    //
    // await waitFor(async () => {
    //     // expect(await result.findByText('')).toBeInTheDocument();
    // });
});
